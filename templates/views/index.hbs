{{!< default}}
<div class="container">
	<div class="jumbotron">
		<h1>Home</h1>

	</div>
	<ul>
	  {{# each products}}
	    <li>
	      <h2>{{{title}}}</h2>
	      <p>{{{shortDescription}}}</p>
	      <a href='{{productUrl slug}}'>Read more</a>
	    </li>
	  {{/each}}
	</ul>
</div>
